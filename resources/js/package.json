{
    "private": true,
    "type": "module",
    "scripts": {
        "format": "prettier --write \"*.js\"",
        "format:check": "prettier --check \"*.js\"",
        "syntax": "node --check PendingRequest.js && node --check bridge.js && node --check fetch.js && node --check request-id.js && node --check retry.js",
        "test": "node --test fetch.test.js"
    },
    "devDependencies": {
        "prettier": "^3.9.6"
    }
}
